import code_after_break from "./code_after_break.test";
import multiple_default_clauses from "./multiple_default_clauses.test";

describe("Invalid switch", async () => {
    code_after_break();

    multiple_default_clauses();
});
